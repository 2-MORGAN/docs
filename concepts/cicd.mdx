---
title: "Pipeline CI/CD"
description: "Comprendre comment CNP gère le cycle build → test → docker pour vos services."
---

## Stages par framework

```mermaid
flowchart LR
    subgraph Go
        direction LR
        G1["build\ngolang:1.22-alpine"] --> G2["test\ngolang:1.22-alpine"] --> G3["docker\nkaniko"]
    end

    subgraph NextJS["Next.js"]
        direction LR
        N1["build\nnode:20-alpine\ncache npm"] --> N2["docker\nkaniko"]
    end

    subgraph NestJS["NestJS"]
        direction LR
        NS1["build\nnode:20-alpine\ncache npm"] --> NS2["docker\nkaniko"]
    end

    subgraph SpringBoot["Spring Boot"]
        direction LR
        S1["test\nmaven:3.9-jdk21"] --> S2["build\nmaven:3.9-jdk21"] --> S3["docker\nkaniko"]
    end

    subgraph Django
        direction LR
        D1["build\npython:3.12-slim\nmanage.py check"] --> D2["docker\nkaniko"]
    end
```

## Build Docker (Kaniko)

```yaml
docker-build:
  stage: docker
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  script:
    - /kaniko/executor
      --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_SHORT_SHA}"
      --destination "${CI_REGISTRY_IMAGE}:latest"
  only:
    - main
```

<Warning>
  Le stage Docker ne s'exécute que sur `main`. Les branches de feature déclenchent uniquement build/test.
</Warning>

## Flux CI/CD → ArgoCD

```mermaid
flowchart LR
    Dev["👨‍💻 Push sur main"] --> CI["GitLab CI\nbuild + test + docker"]
    CI --> Reg["GitLab Registry\nimage:latest"]
    Reg --> ArgoCD["ArgoCD\nsync automatique"]
    ArgoCD --> K8s["Kubernetes\nnouveaux pods"]
    K8s --> Traefik["Traefik Ingress\nIP externe"]
```

## Variables CI disponibles

| Variable | Source | Description |
| --- | --- | --- |
| `CI_REGISTRY` | GitLab (auto) | URL du registry |
| `CI_REGISTRY_USER` | GitLab (auto) | Utilisateur registry |
| `CI_REGISTRY_PASSWORD` | GitLab (auto) | Mot de passe registry |
| `CI_REGISTRY_IMAGE` | GitLab (auto) | Chemin complet de l'image |
| `CI_COMMIT_SHORT_SHA` | GitLab (auto) | SHA court du commit |
| `REGISTRY_TOKEN` | CNP (injecté) | Token personnalisé |
| `REGISTRY_URL` | CNP (injecté) | URL registry personnalisé |
| `REGISTRY_USERNAME` | CNP (injecté) | Utilisateur registry personnalisé |

## Protection de branche

```mermaid
flowchart LR
    Dev["👨‍💻 Developer"] -->|"merge autorisé via MR"| Main(["🔒 branche main"])
    Dev -. "push direct ✗" .-> Main
    Force["⚡ Force push"] -. "désactivé ✗" .-> Main
```

## Déclenchement de pipeline

Lors d'un **redéploiement**, CNP déclenche manuellement un pipeline et re-synchronise ArgoCD.

<Info>
  L'échec du déclenchement de pipeline est loggué en `WARN` mais n'échoue pas la requête. Le scaffold et la sync ArgoCD sont quand même effectués.
</Info>